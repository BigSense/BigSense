package io.bigsense.util

object Crypto {

  
  //Constructor
  // Pull the UA cert from the database. If one does not exist,
  //  create it
  
  
  
  def issueCertificate(customerName : String) = {
    
  }
  
  def issueChallenge(customerName : String) : String = {
    null
  }
  
  def validateChallenge(customerName : String, challenge : String, response : String) = {
    
  }
  
  def revokeCertificate(customerName : String) = {
    
  }
  
  
}